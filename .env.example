# Database connection — used by the backend
# Local dev values shipped via docker-compose.yml
# Copy this file to .env (which is gitignored) and edit if your local setup differs

DB_HOST=localhost
DB_PORT=3306
DB_USER=dev
DB_PASSWORD=dev
DB_DATABASE=scheduling

# Backend
PORT=4000
NODE_ENV=development
